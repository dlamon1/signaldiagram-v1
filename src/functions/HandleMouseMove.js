import { get } from "svelte/store";

import { eRef, ctxRef } from "../store";

export const drawSignalLineWhileMouseMoves = (coordinates, ctx, e) => {
  // let e = get(eRef)
  // console.log(coordinates.origin.x - e.offsetX, coordinates.origin.y - e.offsetY);
  // let ctx = get(ctxRef)
  // console.log(ctx);
  // ctx.beginPath();
  // ctx.lineWidth = 2;
  // ctx.moveTo(coordinates.origin.x, coordinates.origin.y);
  // ctx.lineTo(e.offsetX, e.offsetY);
  // ctx.stroke();
  // ctx.beginPath();
  // ctx.moveTo(0, 0);
  // ctx.lineTo(700, 700);
  // ctx.stroke();
};
